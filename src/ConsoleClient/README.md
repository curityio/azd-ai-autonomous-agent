## Console Client

The console client is a minimal internet application that runs a code flow and uses A2A to call the autonomous agent.  
Users sign in to the application and then send [natural language commands with an access token](src/Program.cs).
